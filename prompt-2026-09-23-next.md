接力 `F:\soft\00selfmade\shgb-keepalive\handoff-2026-09-23-move.md` 的 shgb.cn 挂机任务。

项目已搬离 `E:\shgb-auto\` → `F:\soft\00selfmade\shgb-keepalive\`。用户授权停止后台运行,当前**无** auto-next.mjs 在跑;如用户后续要求重启,在新目录跑 `npm install && node auto-next.mjs`(详见新 handoff §启动方式)。

旧路径 `E:\shgb-auto\` 残留 `node_modules/` 和被 Edge 锁住的 `logs/edge.log`(内容已在新位置),用户决定是否清理。

不要重读 handoff 全文,只看目录定位需要的章节。